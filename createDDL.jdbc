CREATE TABLE transactions (ID  SERIAL NOT NULL, amount INTEGER, created_by VARCHAR(32), created_on TIMESTAMP, edited_by VARCHAR(32), edited_on TIMESTAMP, transaction_type VARCHAR(1), PRIMARY KEY (ID))
